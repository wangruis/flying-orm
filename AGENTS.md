# flying-orm 项目工作流与规范

本文件是 flying-orm 仓库的长期开发规则。Codex、开发代理和协作者进入项目后，应先阅读并遵守这里的约束。

## 项目定位与优先级

flying-orm 是一个独立、全新的 ORM 工具，为动态表单而生。

本项目所有架构、设计、实施落地、审查、Code Review、编码、开发、修复和修改，都必须先符合本项目生命线。
仅仅技术可运行，但违反生命线的方案，不得采用或交付。

设计和实现按以下顺序取舍：

1. 简单、易用、开箱即用。
2. 稳定、安全。
3. 在前两项成立的基础上追求超级高性能、海量并发、海量吞吐和低延迟。

开发阶段避免反复执行全量构建和无关测试，只运行当前改动所必需的少量聚焦测试，验证直接契约和失败边界。
完成一个边界清楚、内部一致的开发批次后，再统一审查修改后的生产代码并执行 Code Review，修复并复核全部发现。
最终交付前按风险和项目规则执行一次完整质量门禁；减少构建或延后审查不能成为绕过生命线的理由。

不能为了跑分牺牲数据正确性、事务语义、默认安全或 API 可理解性，也不能为了并不存在的历史业务和旧 API
兼容要求给新架构增加设计债。

核心方向是轻 SQL、重 Java：动态表单、实体映射、Java DSL、参数条件和前端结构化条件最终进入统一的条件 AST、
SQL 渲染、Scope 安全和参数绑定流程。复杂定制查询优先注册服务端 SQL 模板；临时原生 SQL 仍必须参数化绑定。

## 多模型协作与大类门禁

- `GPT-5.6-Sol` 作为主代理，负责架构决策、任务拆分、风险边界、冲突处理和最终验收。任何子任务结果都不能绕过
  主代理复核直接计入完成进度。
- `GPT-5.6-Terra` 负责边界清楚的中等复杂度实现，例如跨少量内部类型的职责拆分、契约保持和局部重构。
- `GPT-5.6-Luna` 负责范围明确的快速任务，例如代码清点、机械迁移、重复代码清理和局部契约测试。
- 子任务必须提前写清可修改文件、不可改变的行为和验收命令。并行子任务的写入范围必须互不重叠；涉及公共 API、
  SQL、事务、响应式资源生命周期、Scope、安全或缓存语义的改动，必须由主代理逐项复核。
- 后续不得产生新的大类。新增生产类型应控制在 300 个物理源码行和 20 个可调用方法以内；达到任一上限前就要按
  查询、写入、校验、映射、编排、生命周期或基础设施等真实职责拆分。
- 禁止新增或继续扩张达到 400 行或 30 个可调用方法的生产类型。拆分现有大类时，新协作类型同样必须满足门禁，
  不能把代码整体搬进另一个大类，也不能用没有独立职责的转发层、单方法接口或重复同步/响应式实现凑规模指标。
- 每批拆分都要记录原类和新类型的行数、方法数，并用直接相关的契约测试证明 SQL、参数顺序、事务、取消、资源释放、
  错误和观测语义没有变化。没有验证证据的拆分只能算草稿，不能标记为完成。

## 架构边界

- V1.x 以 R2DBC/Reactor 为唯一数据库执行内核；V2.0.0 在保留真响应式链路的同时接入真正的 JDBC 同步执行内核。
  两条执行链必须共享条件 AST、SQL 渲染、方言、参数顺序、Scope、安全校验、codec、错误和批量结果，不能复制 ORM 业务内核。
- 响应式 API 必须保持真正非阻塞，禁止在响应式主链路中调用 `block()`、等待 Future 或包装阻塞式数据库访问。
- V2 同步 API 只使用原生 JDBC，响应式 API 只使用原生 R2DBC。V2 删除 R2DBC 阻塞同步桥接，不保留兼容开关；
  禁止把 JDBC 包装成响应式实现，也不能让同步 JDBC 热路径经过 Reactor。
- flying-orm 本体不依赖 Spring 或其他应用框架，不提供自动配置；但必须提供框架无关的连接与事务参与契约，
  不能把事务一致性问题推给使用方自行规避。
- Spring Boot、依赖注入、动态数据源和上层事务管理属于使用方或外部示例的接入层，不能反向污染核心模块。
  接入层只负责把当前连接、事务和调用上下文交给 flying-orm 的统一契约，不复制 SQL、批量或事务状态机。
- flying-orm 仓库不新增 Spring、starter、自动配置或其他应用框架模块。Spring Boot 接入与验证只放在平级的
  `flying-orm-example` 示例项目中；示例项目不是主项目模块，也不能成为 core/rdb 的依赖。
- 纯 Java、Spring Boot、Micronaut、Quarkus、Jakarta CDI、自研容器和其他 Java 运行环境必须通过同一套框架无关
  契约完成配置、连接、事务、Scope、Repository 和观测装配，不能为某个框架改写 ORM 语义。
- 框架无关接入契约放在现有运行时模块中，保持少量稳定入口，不为每个框架复制 ORM 内核。框架适配代码只存在于
  外部示例或使用方项目，不能让 core/rdb 传递依赖任何应用框架。
- 接入适配器只做五件事：绑定 `flying-orm` 配置、提供连接、桥接当前事务、提供调用上下文、注册统一客户端和
  Repository。SQL 渲染、参数绑定、安全校验、批量和错误模型必须继续由 flying-orm 独立完成。
- 外部容器接入应一次性装配 Executor、FormClient、Repository、DatabaseOperator、Schema 和 Metadata 等统一入口。
  业务代码不应手动创建 renderer、dialect 或 executor，也不应重复传递这些对象。
- 外部示例可以演示继承式 DSL 和按实体泛型注入 `ReactiveRepository<T, ID>` 两种使用方式；两种方式必须共享
  flying-orm 的 Repository、安全配置和事务契约，不能在示例项目里建立第二套 ORM 实现。
- 继承式更新可以提供基于实体绑定 getter 的简写，例如 `set(user::getName)` 和 `where(user::getId)`；它必须通过
  实体元数据解析字段和值，继续执行字段权限、空条件、逻辑删除、Scope 和乐观锁校验，不能变成反射绕过入口。
- 后端可信代码可以直接写单条参数化 SQL。业务值必须使用命名参数绑定，多语句和前端 SQL 仍然拒绝；原生 SQL
  继续复用事务、方言参数标记、codec、超时、行数限制、观测和错误分类。
- 一次数据库调用只能有一个事务控制者。检测到外部事务时，普通 CRUD、原生 SQL、Repository、FormClient 和
  `ATOMIC` 批量必须使用外部事务绑定的同一连接，flying-orm 不得再次 begin、commit 或 rollback。
- 没有外部事务时，普通操作按连接默认提交语义执行；`ATOMIC` 批量由 flying-orm 自己开启、提交或回滚事务。
- `INDEPENDENT` 与外部整批事务语义冲突。外部事务中必须在执行 SQL 前明确拒绝，不能静默加入、挂起外部事务或
  提前提交分片；需要独立提交时应在外部事务之外调用。
- 外部事务中的批量结果只能报告已加入和已执行，不能在外部真正提交前谎报 `COMMITTED`。最终提交或回滚由外部
  事务的完成信号和事务观测事件表达。
- 动态数据源必须在事务开始前完成路由，事务期间不能切换物理数据库；单个本地事务不承诺跨数据库原子性。
- ORM 内部事务结果必须明确表达已提交、已回滚、部分完成或 `UNKNOWN`，让上层能够据此处理业务。
- 不使用隐藏的全局可变状态。运行时对象优先不可变，并明确能否安全并发共享。

## 上层配置约定

- YAML 根配置固定为 `flying-orm`。文档、示例和上层适配层不得改用 `flying.orm`、`flyingOrm` 或其他别名。
- 数据库方言遵循“显式配置优先、缺省自动识别”：配置了 `flying-orm.dialect` 就使用明确方言；没有配置时从
  JDBC URL/metadata、R2DBC URL 或 `ConnectionFactory` metadata 识别。无法识别、识别结果冲突或动态数据源
  方言不一致时启动失败，不能静默降级到某个默认方言。
- 默认 Schema 使用 `flying-orm.default-schema`；MySQL 下表示默认数据库名，运行时已有可信路由结果时以路由结果为准。
- SQL 执行日志默认由上层项目的日志级别管理。开启 `com.flying.orm.sql` 的 DEBUG 后，输出安全的参数化 SQL、参数、
  耗时、返回或影响行数、事务来源和执行方式；慢 SQL、超时、取消、连接异常和 `UNKNOWN` 至少进入 WARN。
- 常规接入不要求配置 `full-sql`、`parameters` 或长度开关。脱敏、LOB/二进制禁止输出、单值截断和消息总长度使用
  内建安全上限；确有特殊需要时才通过框架无关的程序化高级配置调整。
- YAML、Properties、MicroProfile Config 或其他配置系统都映射到同一个类型化配置模型；配置来源可以不同，
  但规范前缀和配置语义不能因框架而改变。
- 动态数据源可以切换物理连接、主从库、Schema 或物理表，但同一应用实例中的全部数据源必须使用最终确定的同一方言。

## 模块边界

| 模块 | 职责 |
| --- | --- |
| `flying-orm-core` | 动态表单模型、条件 AST、Scope、参数条件、通用 SQL 模型、渲染契约和方言 SPI；不依赖具体数据库驱动 |
| `flying-orm-rdb` | JDBC/R2DBC 执行、DDL/DML、FormClient、Repository、Operator、实体映射、codec、缓存和数据库方言 |
| `flying-orm-testkit` | 数据库兼容、事务故障、并发、取消和资源释放等可复用测试工具 |
| `flying-orm-benchmark` | JMH 微基准、真实数据库性能入口和结果比较工具；不让基准代码进入运行时模块 |

不随意改变模块依赖方向。确需调整时，先说明原因、影响范围和替代方案，再同步架构文档或 ADR。

## 技术基线

- Java 21。
- Maven 3.9 或更高版本。
- Reactor、R2DBC SPI，以及 Java 自带的 JDBC API。
- Caffeine 用于有界高并发缓存。
- JUnit Jupiter、JaCoCo、Checkstyle 和 SpotBugs 组成代码质量门禁。
- JMH 和固定真实数据库脚本用于性能验证。

Java 21 的使用原则：

- 简单不可变数据优先使用 `record`，有限状态集合优先使用 `enum`。
- 只读共享数据优先不可变，避免防御性复制和隐式并发风险。
- 模式匹配等语言特性应服务于清晰建模，不为使用新语法而降低可读性。
- 虚拟线程只适合同步调用边界或其他明确的阻塞任务，不能把它当作响应式数据库内核。
- 引入线程、队列或并发容器前，必须说明生命周期、容量、背压和关闭方式。

## 代码发现与影响分析

当前开发环境提供 `codebase-memory-mcp`。查找代码定义、调用关系和架构边界时，按以下顺序使用：

1. `search_graph`：查找类、方法、变量和入口。
2. `trace_path`：确认调用方、被调用方和影响范围。
3. `get_code_snippet`：读取已经定位的类或方法。
4. `query_graph`：处理复杂关系、热点和复杂度查询。
5. `get_architecture`：了解模块、依赖和整体结构。

索引不存在或明显过期时，使用 `index_repository` 更新。只有在 MCP 不可用、索引结果不足，或者需要查找字符串、
配置和非代码文件时，才回退到 `rg`、文件读取和 Maven 命令。

修改公共 API、共享条件逻辑、SQL 渲染、事务、批量、缓存或执行器前，必须先检查调用关系和测试覆盖面。不要为了
完成局部任务盲目扫描或重写整个仓库，也不要把无关的死代码清理混入功能改动。

## 公共 API 规则

- 公开 API 以简单、安全和难以误用为目标，不用大量组合重载表达 Scope、执行保护和乐观锁。
- 常用路径保留短入口；高级组合通过 `QuerySpec`、`WriteSpec`、`BatchSpec` 或链式命令表达。
- 不公开只为包内协作服务的实现类、构造器和辅助对象。
- 不提供能够绕过统一方言、Scope、参数绑定、执行保护或错误分类的快捷入口。
- 原生 SQL 入口必须使用命名参数，拒绝缺失参数、多余参数和多语句；名称中的 `unsafe` 表示 SQL 结构由使用方审核，
  不表示允许拼接业务值。
- 注册 SQL 模板只用于查询。模板正文只能来自启动阶段可信代码或配置，租户、用户等服务端安全参数不能由普通
  `bind(...)` 覆盖；分页和统计使用显式模板，不解析或猜测任意复杂 SQL。
- 修改 V1 公开类型或方法时，必须同步公共 API 基线、使用示例和相关契约测试。
- V1.0.0 仍允许用户明确要求的破坏性优化，但不得以“兼容未知现有业务”为理由保留重复入口。
- V2 实体映射使用 `com.flying.orm.core.annotation` 下的 flying-orm 自有注解，不依赖或反射读取 Jakarta Persistence。
  注解名称可以贴近 Java ORM 用户熟悉的 `TableName`、`TableId`、`TableField`、`Version`、`TableLogic`、
  `EnumValue` 和 `KeySequence`，但实现、元数据模型和安全语义必须保持独立。
- 主键生成必须显式可知：`IdType.AUTO` 由数据库自增，`INPUT` 由调用方赋值，`ASSIGN_ID` 和 `ASSIGN_UUID` 由 ORM
  在 SQL 前生成。雪花 ID 不得成为未配置的隐藏默认值，worker/node id 不可靠时必须拒绝启动或生成。
- 数据库主键生成不能只停留在 DDL；insert 必须读取并返回生成键。不可变实体不能通过反射强行回写。
- 不接受把原始 SQL 字符串藏进实体默认排序注解；排序使用类型安全 DSL 或启动期注册并审核的 SQL 模板。
- V2 可以提供字段级 `@OrderBy(asc, sort)`，但只能引用当前已映射字段，显式 DSL 排序优先，注解不能接受 SQL 片段。
- 启动期实体扫描和 Schema 同步必须复用 DynamicForm、Schema diff、DDL 审核与缓存失效，不能建立第二套 DDL 引擎。
  默认关闭；校验、安全更新和破坏性更新必须是清晰分级的显式策略，危险 DDL 不能绕过批准指纹。
- 自动 Schema 同步只增加启动编排，不得改变现有 DDL/DML、Repository、FormClient 或 DatabaseOperator 的使用入口。

## 动态表单与结果模型

- `DynamicForm` 是 DDL、CRUD、批量、类型校验、结果解码和元数据同步的统一运行时模型。
- 动态查询默认返回 `DynamicRow`。它仍是只读 `Map<String, Object>`，但结果集共享列布局，每行只保存值数组。
- 普通读取不要主动调用 `DynamicRow.toMap()`；只有明确需要独立普通 Map 时才物化。
- 实体 Repository 复用动态表单执行内核，不建立第二套 SQL、安全或事务实现。
- 动态字段类型转换必须通过统一 codec 注册机制，写入、条件参数和结果读取不得各自维护转换规则。
- 动态改表后必须精确失效相关元数据和 SQL 结构缓存，不能依赖长时间自然过期。

## 条件与安全规则

- 所有业务值必须参数化绑定。表名、字段名和 SQL 标识符必须通过结构化模型和方言校验。
- 前端只能透传结构化条件，不能透传 SQL。
- 结构化条件必须校验字段、operator、值类型、嵌套深度、节点数、集合大小和字符串长度，并返回稳定错误码与路径。
- 租户字段、逻辑删除字段和受保护字段不能由前端条件伪造。
- 普通 `where(...)` 使用严格空值语义；允许缺省的搜索项必须显式使用 `whereIfPresent(...)`。
- 更新和删除必须包含明确业务条件。TenantScope、DataScope、FieldScope、TimeScope、逻辑删除和乐观锁只能继续收窄
  范围，不能替调用方补出业务条件。
- 无租户系统可以只使用 `DataScope`；SaaS 系统使用 `TenantScope + DataScope`。
- 乐观锁是显式能力；实体声明 `@Version` 后，Repository 和实体 DML 按版本字段执行冲突检查。
- 默认删除优先遵守逻辑删除定义，物理删除必须通过明确入口调用。
- 错误消息不能包含密码、完整连接串、敏感参数值或无法控制大小的原始数据。
- 数据迁移、补偿、批量和 Schema 结果中的公开失败说明只使用稳定分类；底层驱动异常原文不能复制进公开结果对象。

## 批量、内存与缓存

- 批量写入默认使用 `ATOMIC`：整批原子提交，失败全部回滚。
- `INDEPENDENT` 必须显式开启，分片分别提交，并返回每个分片及最终汇总状态。
- 批量输入优先接收 `Publisher` 并按有界 chunk 消费，禁止为方便处理而提前把整个流收集进内存。
- `Publisher<Object[]>` 上游允许复用参数数组；JDBC/R2DBC 必须在接收每一行时立即保存独立快照，不能等整片形成后再复制，也不能把调用方数组长期保留在结果或缓存中。
- 批量必须限制最大输入行数、单分片大小、并发数、连接等待和执行时间。
- 驱动、事务内生成键回调或提交前生命周期回调抛出 `RuntimeException`/`Error` 时，事务和资源清理规则不能被绕过；自有事务必须先确认回滚，无法确认时返回 `UNKNOWN` 并隔离连接。
- `UNKNOWN` 不能伪装成成功或普通失败，必须保留 recovery token，并提供幂等恢复查询。
- 元数据、SQL 结构计划、条件编译和实体映射缓存必须有容量上限、淘汰策略、统计和精确失效能力。
- 禁止新增无上限 `ConcurrentHashMap` 作为长期缓存，也不能因为上层可能有 Caffeine 就关闭 ORM 自己的内部缓存治理。
- 热路径避免每行创建重复 Map、列名、反射模型和中间集合；优化前后都要用基准或分配数据验证。

## 性能与并发

- 性能优化不能修改事务持久性、降低安全校验、吞掉异常或放宽既定门禁。
- 查询、更新、批量、连接获取和 LOB 读取必须有明确的超时与容量保护，并在取消后释放连接和驱动资源。
  R2DBC 的连接获取由 ORM 限时；标准 JDBC 的连接等待由 DataSource/连接池限时，ORM 不用隐藏线程伪造
  无法可靠取消的 `getConnection()` 超时。
- 响应式链路必须尊重背压，避免无界 `flatMap`、无界预取和不受控制的并发订阅。
- 共享客户端、渲染器、方言、codec 注册表和缓存策略应不可变或具备明确线程安全保证。
- 基准结果必须记录数据库版本、驱动版本、参数、样本数量、P95/P99、吞吐、错误数和连接泄漏情况。
- 一次偶然跑分快不能作为结论。性能门禁至少使用预热后的多轮中位数，并区分代码问题和数据库环境问题。

## 数据库支持

- H2：开发和自动测试基线。
- MySQL：正式支持，实库认证主版本为 MySQL 8.4。
- PostgreSQL：正式支持，包含原生 Array、数组条件和 pgvector。
- Oracle：正式支持，代码边界覆盖 12c 至 23ai，实库认证使用 Oracle Free 23；JDBC/R2DBC 的功能、事务、故障、取消和并发边界均纳入认证。
- SQL Server：正式支持，代码边界覆盖 2012 至 2022，实库认证使用 SQL Server 2022。
- OpenGauss：不支持，当前不规划。

“代码中存在方言”不等于“已经实库认证”。修改数据库特有 DDL、DML、类型、异常或事务行为后，应同步支持矩阵，
并在发版前复跑对应真实数据库认证入口。

## 编码与注释

- 保持实现直接、职责清楚，优先复用现有模式，不为了形式上的 SOLID 引入没有实际价值的抽象。
- 新增或修改代码后，清理无用 import、无用变量、重复校验和已经失效的内部协作代码。
- 默认使用 ASCII 文件名、包名和标识符；中文用于注释、文档和协作说明。
- Java 类、接口、枚举、record、公开方法、构造器、配置对象、关键内部算法、测试类和测试方法应有自然中文
  Javadoc 或注释。
- 类型级 Javadoc 的作者固定为 `wangr`，日期使用创建或本次实质调整日期，版本默认写 `v1.0`。
- 注释说明用途、边界、失败语义、线程安全和为什么这样做，不复述赋值、循环等一眼可见的代码。
- 复杂参数、返回值和异常使用 `@param`、`@return`、`@throws` 说明。测试注释说明验证的契约，不逐步复述测试代码。
- 不保留官样、空泛、过期或与实现矛盾的注释。

## 文档与需求

- 中文是默认协作语言，除非用户明确要求其他语言。
- `README.md` 面向使用者，优先展示定位、上手方式、安全边界、数据库支持和文档导航，不写本机路径或开发过程流水账。
- `docs/requirements/` 是需求与重要决策的记忆层，`docs/requirements/index.md` 应保持可追踪。
- 公共 API、数据库支持、错误码、认证方法和已知限制发生变化时，必须同步对应文档。
- 文档中不臆想上层业务，不写为了未知“现有系统兼容”而做出的设计。
- 设计来源、内部对比和临时研究可以用于开发判断，但不应把项目说明绑定到其他 ORM。

## 构建与验证

验证强度与风险匹配。普通注释、文档和局部低风险调整不必运行全量测试；公共 API、条件安全、事务、批量、
缓存和执行器修改必须扩大验证范围。

常用门禁：

```shell
# 编译全部模块
mvn -DskipTests package

# 单元测试、覆盖率、Checkstyle 和 SpotBugs
mvn -Pquality clean verify

# 源码包、Javadoc 和发布制品检查
mvn -Prelease-artifacts verify
```

- 真实数据库认证和性能测试不默认进入普通 Maven 构建。固定版本、场景和证据要求见
  `docs/real-database-certification.md`；本地 Docker 编排和认证脚本单独维护，不属于源码仓库交付内容。
- 测试失败时先定位根因，不通过删除断言、扩大容差或跳过高风险测试来制造绿色结果。
- 用户明确要求少量测试时，保留与改动直接相关的最小契约测试；高风险逻辑仍必须验证失败边界。
- 无法执行的验证必须在交付说明中写清楚，不能表述为已经通过。

## 变更与 Git 约束

- 先理解需求和现有实现，再修改代码；不要只给计划而停在未完成状态。
- 修改文件前说明准备改什么，持续任务中及时汇报已经确认的事实和下一步。
- 不修改与当前任务无关的代码，不顺手格式化整个项目。
- 不删除、覆盖或回退用户已有变更。遇到同文件并行修改时，理解并合并现状；只有无法安全继续时才询问。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求并批准。
- 不初始化 Git、不创建分支、不提交、不推送，也不改写历史，除非用户明确要求。
- 提交时只暂存当前任务文件，提交说明使用自然、清楚的大白话中文。
- 不提交构建输出、真实数据库凭据、临时验证目录、运行日志或本机专用配置。
- 完成后说明改动、验证、未验证边界和仍留在工作区但不属于本任务的内容。
