/**
 * 查询、写入和批量执行的轻量观测钩子与只读事件模型。
 *
 * <p>事件记录 SQL 类型、耗时、影响行数、分片位置、批量状态和稳定异常分类。Observer 运行在执行链路上，
 * 实现必须快速、非阻塞，并自行隔离日志或指标系统故障，不能因为观测失败改变数据库操作结果。</p>
 *
 * <p>默认不记录参数值，避免把密码、令牌和业务隐私带入日志。确需记录时应由上层做字段级脱敏。</p>
 */
package com.flying.orm.rdb.observation;
