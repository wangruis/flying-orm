/**
 * 批量写入的模式、分片计划、状态、回执和恢复模型。
 *
 * <p>默认 {@code ATOMIC} 模式把整批数据放在一个事务里，任一分片失败都会回滚整批；显式
 * {@code INDEPENDENT} 模式允许分片独立提交，并在结果中同时返回成功和失败分片。提交结果无法确认时
 * 使用 {@code UNKNOWN}，上层必须根据 recovery token 或业务幂等键核对，不能把它直接当成失败重试。</p>
 *
 * <p>批量结果对象构造后只读，可以安全地交给观测、日志和业务补偿流程。</p>
 */
package com.flying.orm.rdb.batch;
