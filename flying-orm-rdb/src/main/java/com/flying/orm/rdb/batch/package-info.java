/**
 * 批量写入的模式、分片计划、状态、回执和恢复模型。
 *
 * <p>默认 {@code ATOMIC} 模式只参与上层外部事务，非空批次缺少事务时拒绝；空批次保留无工作结果。
 * 显式 {@code INDEPENDENT} 模式允许 ORM 拥有分片局部事务，结果保留此前已提交分片的事实。
 * 提交结果无法确认时使用 {@code UNKNOWN}；核对、幂等、回执和事务恢复由上层负责，不能直接当成失败重试。</p>
 *
 * <p>批量结果对象构造后只读，可以安全地交给观测、日志和业务补偿流程。</p>
 */
package com.flying.orm.rdb.batch;
