/**
 * 动态表单 DDL、结构差异、迁移计划和执行结果。
 *
 * <p>默认安全策略只自动执行明确可控的结构变化。删列、危险类型变更、主键和外键重构必须显式放开并由
 * 上层审核迁移计划。DDL 成功后会精确失效当前进程的表元数据缓存，多实例通知由上层基础设施负责。</p>
 *
 * <p>不同数据库的语法差异由 Schema 方言处理，业务代码只描述目标结构，不拼接数据库专用 DDL。</p>
 */
package com.flying.orm.rdb.schema;
